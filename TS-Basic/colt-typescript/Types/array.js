// 배열의 annotatino은 타입을 설정하고 []을 작성한다.
var activeUsers = ["colt"];
activeUsers.push("Tony");
// activeUsers.push(21);  type error!!
// const bools: boolean[] = []
var bools = []; // 위와 밑은 똑같은 구문이다.
var coords = [];
coords.push({ x: 23, y: 8 });
console.log(coords);
